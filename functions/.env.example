# Required in production. Comma-separated accounts that receive equivalent
# administrator access. Copy this file to functions/.env.<firebase-project-id>
# before deploying; never commit the real values.
ADMIN_EMAILS=owner@example.com,second-admin@example.com

# Optional; Pancita, Pancite and Panza are always accepted as equivalent public aliases.
CASE_SLUG=pancita
