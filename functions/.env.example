# Required in production. This is the only account allowed to bootstrap owner access.
# Copy to functions/.env.<firebase-project-id> before deploying; never commit the real value.
OWNER_BOOTSTRAP_EMAIL=owner@example.com

# Optional; Pancita, Pancite and Panza are always accepted as equivalent public aliases.
CASE_SLUG=pancita
